server:
  port: 8080

spring:
  application:
    name: backend-test

  datasource:
    url: jdbc:postgresql://localhost:5432/vsa_db_test
    username: vsa_user
    password: vsa_password
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  mail:
    host: localhost
    port: 2525
    username: test@vsa.com
    password: testpassword
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

jwt:
  secret: "XrvacgnP6R03xIstugi0vnEV+6xPySC7Rf1V7X3r9kM="
  expiration: 86400000

aws:
  region: ${AWS_REGION:us-east-2}
  s3:
    bucket-name: ${S3_BUCKET_NAME:test-vsa-event-images}
  cloudfront:
    domain: ${CLOUDFRONT_DOMAIN:https://images.example.com}
frontend:
  url: http://localhost:5173